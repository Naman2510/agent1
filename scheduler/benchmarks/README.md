# scheduler/benchmarks
Benchmark workload definitions and the harness that runs them on both the CPU and accelerator to produce the AI scheduler's real, measured training data (Phase 11, consumed starting Phase 13).
