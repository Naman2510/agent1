# scheduler/runtime
The Phase 15 dynamic-scheduling demo: a single RISC-V program that computes the AI scheduler's CPU-vs-accelerator decision at runtime, in hardware/software, for a stream of workloads -- plus the static baselines (always-CPU, always-accelerator, oracle) it's measured against.
